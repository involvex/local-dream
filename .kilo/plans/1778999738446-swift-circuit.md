# Plan: Enhance Prompt Templates and Add New Features

## Goal
Enhance the local dream app with improved prompt templates and additional features.

## Key Files Identified
- `TagModels.kt` - Tag/prompt system
- `Preferences.kt` - Per-model settings
- `BackgroundGenerationService.kt` - Generation pipeline
- `ImageUtils.kt` - Image processing
- `ModelRunScreen.kt` - Main generation UI
- `HistoryManager.kt` / `HistoryDao.kt` - History management
- `ParamShare.kt` - Parameter sharing
- `TagAutocompleteRepository.kt` - Tag suggestion logic
- `PromptTagTextField.kt` - Tag input field
- `ParamShareDialogs.kt` - Share parameter dialogs
- `Model.kt` - Model repository

## What's been done so far
- Explored codebase structure
- Understood generation pipeline and UI screens
- Reviewed tag-based prompt system, history management, parameter sharing
- ✅ Enhanced Prompt Templates with Categories (PromptTemplates.kt + UI)
- ✅ Batch Generation support (batchCount in params + sharing)
- ✅ Prompt Variation Generator (PromptVariationGenerator.kt + UI dialogs)
- ✅ Image Comparison Tool (ImageCompareScreen.kt + navigation)
- ✅ Fixed compilation errors (RadioButton import, exhaustive when expressions for BATCH_COUNT)
- ✅ Prompt History Tracking - already implemented (HistoryFilter.promptSubstring + UI search field)

## Next Steps - Enhancement Plan

### 1. Enhanced Prompt Templates with Categories
Add category-based prompt templates to help users quickly start generations.

**Files to modify**:
- Create `data/PromptTemplates.kt` - Define template categories (Anime, Realistic, Landscape, Portrait, etc.) with pre-built prompts
- Update `ModelRunScreen.kt` - Add template selection UI
- Update `Preferences.kt` - Store selected template per model

### 2. Batch Generation Feature
Allow generating multiple images at once.

**Files to modify**:
- `ModelRunScreen.kt` - Add batch count selector, batch generation logic
- `BackgroundGenerationService.kt` - Support batch requests
- `ParamShare.kt` - Include batch count in sharing

### 3. Prompt Variation Generator
Generate variations of current prompt with different styles.

**Files to modify**:
- Create `utils/PromptVariationGenerator.kt` - Generate prompt variations
- Update `ModelRunScreen.kt` - Add variation UI

### 4. Image Comparison Tool
Compare generated images side by side.

**Files to modify**:
- Create `ui/screens/ImageCompareScreen.kt` - Side-by-side comparison
- Update navigation in `Navigation.kt`

### 5. Prompt History Tracking
Track and recall previously used prompts.

**Files to modify**:
- Update `HistoryManager.kt` - Store prompt history
- Update `HistoryDao.kt` - Add prompt substring search
- Update `HistoryFilterUI.kt` - Add prompt search filter

## Implementation Order
1. Create prompt templates system
2. Add batch generation support
3. Implement prompt variations
4. Add image comparison
5. Enhance history with prompt tracking
